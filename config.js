const CONFIG = {
  mouthThreshold: 20,      // Distance between lips
  proximityThreshold: 150, // Distance between eyes (simulates depth)
  smoothing: 0.2,          // For lerping values if needed
  debugMode: true          // Show landmarks/labels
};
